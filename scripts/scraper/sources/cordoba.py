"""
Córdoba tourism scraper — STUB.

The provincial camping registry was hosted on a Google Sheets document
that is no longer publicly accessible. OSM already covers ~251 campings
in Córdoba province.

If a public data source becomes available in the future, implement
scrape() following the same convention as mendoza.py/rionegro.py.
"""

from __future__ import annotations

from pathlib import Path


def scrape(cache_path: Path | None = None) -> list[dict]:
    """No public data source available. Returns empty list."""
    return []
